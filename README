This is the readme, SlamDunkn plz amend it as required.

Dependancies:
python-dev, python2-qt4, build-essential, socksipy

To get the dependancies in debian based distributions (including Ubuntu and Linxu Mint)
run:

sudo apt-get install python-dev python-qt4 python-socksipy build-essential


to build run:
make


to rebuild run:
make clean; make


